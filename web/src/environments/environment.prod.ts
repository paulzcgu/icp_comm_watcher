export const environment = {
  production: true,
  hdrSubscriberApiUrl: 'http://localhost:5000/api/',
  hdrSubscriberApiDomain: 'localhost:5000',
  version: require('../../package.json').version,
  name: require('../../package.json').product
};
